# Trae CLI Headless Bridge

This companion extension starts a localhost bridge inside Trae's extension host
so the external `traecli` harness can inspect and execute internal VS Code /
Trae commands without GUI DOM automation.
